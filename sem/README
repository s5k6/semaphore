Contents

    /src — sources for the ‘sem’ binary.
    /doc — documentation for the ‘sem’ binary.
    /acc — accessory scripts that may be useful with ‘sem’.


Available make targets:

    make all       # makes binary and documentation

    make dist      # creates package file

    make clean     # remove created files

    make install   # install binaries and docs
